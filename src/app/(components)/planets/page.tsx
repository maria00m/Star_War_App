import PlanetDetails from "../planetsDetails/page";

/**
 * Planets page wrapper component
 * Renders the StarshipList component for the /planets route
 * @returns {JSX.Element} Planets page with PlanetsList component
 */
export default function Planets(){
    return <>
   <PlanetDetails/>
    </>
}