import CharacterDetails from "../characterDetails/page";


/**
 * Characters page wrapper component
 * Renders the StarshipList component for the /characters  route
 * @returns {JSX.Element} Characters page with CharacterList component
 */
export default async function Characters(){


    return <>
   <CharacterDetails/>
    </>
}

