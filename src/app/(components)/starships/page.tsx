import StarshipList from "../starShipDetails/page";

/**
 * Starships page wrapper component
 * Renders the StarshipList component for the /starships route
 * @returns {JSX.Element} Starships page with StarshipList component
 */
export default function Starships(){
    return <>
   <StarshipList/>
    </>
}