import FilmDetails from "../filmDetails/page";

/**
 * Films page wrapper component
 * Renders the StarshipList component for the /films route
 * @returns {JSX.Element} Films page with FilmsList component
 */
export default function Films(){
    return <>
    <FilmDetails/>

    </>
}