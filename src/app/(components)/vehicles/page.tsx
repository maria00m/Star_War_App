import VehicleDetails from "../vehiclesDetails/page";

/**
 * Vehicles page wrapper component
 * Renders the StarshipList component for the /vehicles route
 * @returns {JSX.Element} Vehicles page with VehiclesList component
 */
export default function Vehicles(){
    return <>
    <VehicleDetails/>
  </>
}